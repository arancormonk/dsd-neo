rules:
  # Current policy: every external action must at least use an explicit tag,
  # branch, or SHA. Hash-pinning remains the preferred release-workflow end
  # state, but enabling that as a hard gate would fail the current workflows.
  unpinned-uses:
    config:
      policies:
        "*": ref-pin
